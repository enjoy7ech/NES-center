# RetroArch Core Assets

This directory contains the pinned Emscripten build of the RetroArch FCEUmm
libretro core used by the application. The React application loads the core
directly and owns the canvas, input, ROM lifecycle, and controls.

Keep the corresponding RetroArch, Nestopia, and Emscripten license notices with
any redistribution of these generated assets.
